import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'it35c-mobile application',
  webDir: 'dist'
};

export default config;
